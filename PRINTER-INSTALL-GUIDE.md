# คู่มือติดตั้ง Siatoy TCG Printer Helper

คู่มือนี้ใช้สำหรับเครื่องพิมพ์ฉลาก **4BARCODE 4B-2054** ที่ต่อผ่าน USB กับ Windows และกระดาษฉลาก 3 ช่อง ขนาดดวงละ **32 × 25 มม.** ตัวช่วยพิมพ์จะทำงานบนเครื่องคอมพิวเตอร์ที่ต่อเครื่องพิมพ์ แม้หน้า POS จะเปิดจาก Vercel หรือจากมือถือก็ตาม

## สิ่งที่ต้องเตรียม

1. คอมพิวเตอร์ Windows ที่ต่อเครื่องพิมพ์ด้วย USB
2. เครื่องพิมพ์ 4BARCODE 4B-2054 เปิดเครื่องและเสียบสาย USB
3. ไฟล์แพ็กเกจ `Siatoy-Printer-Installer.zip`
4. ไดรเวอร์ 4BARCODE (มีไฟล์ `4BARCODE_2024.2_M-3.zip` รวมอยู่ในแพ็กเกจ หากจัดแพ็กเกจไว้ครบ)
5. อินเทอร์เน็ตสำหรับเปิดหน้า POS ครั้งแรก

> ห้ามติดตั้งตัวช่วยพิมพ์บนโทรศัพท์ เพราะโทรศัพท์เข้าถึงเครื่องพิมพ์ USB ของคอมพิวเตอร์ไม่ได้

## 1. ติดตั้งไดรเวอร์เครื่องพิมพ์

1. แตกไฟล์ `Siatoy-Printer-Installer.zip` ไปไว้ในโฟลเดอร์ที่เขียนได้ เช่น `Downloads\Siatoy-Printer` 
2. หากตัวติดตั้งเปิดไดรเวอร์ให้อัตโนมัติ ให้ทำตามหน้าจอของผู้ผลิตจนเสร็จ
3. หากไม่เปิดอัตโนมัติ ให้แตก `vendor\4BARCODE_2024.2_M-3.zip` แล้วเปิดไฟล์ติดตั้งภายใน
4. เสียบ USB เครื่องพิมพ์และเปิด **Settings → Bluetooth & devices → Printers & scanners**
5. ตรวจว่ามีเครื่องพิมพ์ชื่อ **4BARCODE 4B-2054** และพอร์ตเป็น USB (ตัวอย่างเช่น USB002)
6. พิมพ์ Windows Test Page หนึ่งครั้งเพื่อยืนยันว่าไดรเวอร์มองเห็นเครื่อง

ถ้า Windows ไม่พบเครื่อง ให้เปลี่ยนช่อง USB ตรวจสาย และติดตั้งไดรเวอร์ใหม่ด้วยสิทธิ์ Administrator

## 2. ติดตั้ง Siatoy Printer Helper

1. เปิดโฟลเดอร์ที่แตกไฟล์ไว้
2. ดับเบิลคลิก `install\Install-SiatoyPrinter.cmd`
3. อนุญาตหน้าต่าง PowerShell หาก Windows ถาม
4. ตัวติดตั้งจะคัดลอกไฟล์ไปที่ `%LOCALAPPDATA%\SiatoyPrinter`
5. จะสร้างทางลัด **Siatoy Printer Helper** ใน Start Menu และเปิดตัวช่วยพิมพ์ให้อัตโนมัติ

หากแพ็กเกจไม่มี Node.js ในโฟลเดอร์ `runtime` ต้องติดตั้ง Node.js LTS ก่อน แล้วจึงรันไฟล์ติดตั้งใหม่

## 3. ตรวจสอบตัวช่วยพิมพ์

เปิดเบราว์เซอร์บนเครื่องคอมพิวเตอร์ แล้วเข้า:

`http://127.0.0.1:18767/`

ควรเห็นหน้า **Siatoy Printer Helper** และรายละเอียดเครื่องพิมพ์/ขนาดฉลาก หากเข้าไม่ได้ ให้เปิดทางลัด **Siatoy Printer Helper** จาก Start Menu หรือดับเบิลคลิก `Start-Printer.cmd` ใน `%LOCALAPPDATA%\SiatoyPrinter`

ตัวช่วยพิมพ์ใช้พอร์ตเฉพาะเครื่อง `127.0.0.1:18767` จึงไม่ต้องเปิดพอร์ตเราเตอร์หรือ Firewall ให้อินเทอร์เน็ตเข้าถึง

## 4. ตั้งค่ากระดาษในไดรเวอร์

ใช้ค่าของสต๊อกกระดาษที่ติดตั้งไว้ในไดรเวอร์ 4BARCODE ตามที่เคยคาลิเบรตแล้ว:

- กระดาษ 3 ช่อง แนวนอน
- ขนาดฉลากแต่ละดวง 32 × 25 มม.
- จำนวนคอลัมน์ 3
- ช่องว่างระหว่างดวงประมาณ 2 มม.
- ความละเอียด 203 dpi

อย่าสร้าง Windows Paper Size แบบ 102 × 25 มม. แทนสต๊อกของไดรเวอร์ เพราะค่าดังกล่าวเป็นความกว้างรวมที่ใช้วัดจากไดรเวอร์ ไม่ใช่ขนาดดวงฉลาก

## 5. พิมพ์จาก Siatoy POS บน Vercel

1. เปิดตัวช่วยพิมพ์บนคอมพิวเตอร์ให้ทำงานค้างไว้
2. เปิด [Siatoy POS](https://siatoy-pos.vercel.app/)
3. เข้าสู่ระบบด้วยบัญชีของร้าน หรือเติม `?demo=1` หลังโดเมนเพื่อใช้โหมดทดลอง
4. เปิดเมนู **Labels/ป้ายฉลาก**
5. กรอกรหัสสินค้า 3 รหัส หรือสแกนด้วยเครื่องยิงบาร์โค้ด
6. กด **พิมพ์**
7. อนุญาตหน้าต่าง Printer Helper ที่เปิดขึ้นมา หากเบราว์เซอร์ถาม
8. ตรวจรายการในหน้าต่าง Helper แล้วกดปุ่มพิมพ์

เว็บ Vercel จะส่งงานไปยัง Helper ที่เปิดอยู่บนเครื่องเดียวกับเบราว์เซอร์ผ่าน `127.0.0.1` จากนั้น Helper จึงสั่งพิมพ์ไปยัง USB printer

## 6. ทดสอบก่อนใช้งานจริง

พิมพ์รหัสทดสอบ 3 ดวง เช่น `TOY01`, `TOY02`, `TOY03` แล้วตรวจว่า:

- ออกครบ 3 ช่องในหนึ่งแถว
- ข้อความและบาร์โค้ดอยู่กึ่งกลางดวง
- บาร์โค้ดอ่านได้ทั้งสามดวง
- ไม่มีการเลื่อนแถวหรือข้ามดวง

หากเพิ่งเปลี่ยนม้วนกระดาษ ให้ทำการ Calibrate/สั่ง Feed ตามคู่มือเครื่องก่อนพิมพ์จริง

## แก้ปัญหาที่พบบ่อย

### ขึ้น `ERR_CONNECTION_REFUSED` ที่ 127.0.0.1:18767

ตัวช่วยพิมพ์ยังไม่ทำงาน ให้เปิด `Start-Printer.cmd` หรือทางลัดใน Start Menu แล้วลองใหม่

### เว็บแจ้งว่าเชื่อมตัวช่วยพิมพ์ไม่ได้

ตรวจว่าเปิด POS และ Printer Helper ในเบราว์เซอร์เครื่องเดียวกัน และไม่มีหน้าต่าง Helper ถูกบล็อกเป็น Popup

### มองเห็น Helper แต่พิมพ์ไม่ออก

ตรวจชื่อเครื่องพิมพ์ใน Windows ให้เป็น **4BARCODE 4B-2054**, ตรวจสาย USB และลอง Windows Test Page จากนั้นเปิด Helper ใหม่

### พิมพ์เหลื่อมหรือไม่ตรงช่อง

ตรวจสต๊อกกระดาษในไดรเวอร์และทำ Calibrate ใหม่ อย่าเปลี่ยนค่าขนาดฉลากใน `printer-profile.json` เองโดยไม่ทดสอบสแกน

### บาร์โค้ดอ่านไม่ได้

ตรวจว่าพิมพ์ที่ 203 dpi ใช้ Code 128 และมีพื้นที่ว่างด้านซ้าย/ขวาของบาร์โค้ด ห้ามย่อจนชิดขอบฉลาก

## การปิดและเปิดใหม่

ปิดหน้าต่าง Helper ได้เมื่อเลิกใช้งาน หากต้องการพิมพ์ครั้งถัดไปให้เปิดทางลัด **Siatoy Printer Helper** ใหม่ ตัวช่วยไม่ได้ทำงานเป็นบริการ Windows และจะไม่เปิดเองหลังรีสตาร์ท เว้นแต่ตั้งค่า Startup เพิ่มภายหลัง

## ไฟล์สำคัญสำหรับผู้ดูแลระบบ

- `tools/printer/Siatoy-Printer-Installer.zip` แพ็กเกจแจกให้ลูกค้า
- `tools/printer/install/Install-SiatoyPrinter.cmd` ตัวติดตั้ง
- `tools/printer/Start-Printer.cmd` เปิด Helper
- `tools/printer/printer-profile.json` ค่าคาลิเบรตเครื่องพิมพ์
- `tools/printer/print-labels.ps1` ตัวสั่งพิมพ์ฉลากแบบ native

ไม่ควรแก้ไขไฟล์โปรไฟล์หรือสคริปต์บนเครื่องลูกค้าโดยตรง ให้สร้างแพ็กเกจรุ่นใหม่แล้วติดตั้งทับเพื่อให้ทุกเครื่องใช้ค่าคาลิเบรตชุดเดียวกัน
